name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch: {}

jobs:
  hello:
    name: Hello world
    runs-on: ubuntu-latest
    steps:
      - name: Print hello
        run: echo "hello"
