;
; logger.conf — destino y niveles de logs
;
; En contenedor escribimos a console (stdout/stderr) para que docker logs
; capture todo; el file 'messages' queda para inspección manual del volumen.
;

[general]

[logfiles]
console => notice,warning,error,verbose(3)
messages => notice,warning,error
